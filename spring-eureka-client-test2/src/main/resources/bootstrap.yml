# bootstrap.yml先于application.yml加载，一般不变的东西写在bootstrap里
spring:
  application:
    name: test2  #为你的应用起个名字，该名字将注册到eureka注册中心


#为了Eureka可以区分两个相同host的相同服务
#eureka:
#     instance:
#       instanceId: ${spring.cloud.client.hostname}:${spring.application.name}:${spring.application.instance_id:${random.value}}
